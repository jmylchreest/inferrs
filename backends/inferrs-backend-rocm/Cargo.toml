[package]
name = "inferrs-backend-rocm"
version = "0.1.0"
edition = "2021"
description = "ROCm/HIP runtime probe plugin for inferrs"
license = "Apache-2.0"

[lib]
name = "inferrs_backend_rocm"
crate-type = ["cdylib"]

[dependencies]
hiparc = { workspace = true }
